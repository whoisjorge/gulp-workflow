# Gulpjs 'basic' workflow
Just run <code>npm install --only=dev</code> and then <code>**gulp**</code> to serve files.

### Features

- SASS preprocessor
- Compress CSS
    1. Prefix CSS
- Compress JavaScript
- Minify HTML
- Compress Images
- Browser sync.


